# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: login-page.spec.ts >> Login Page - Professional UI Tests >> Loading States >> should show loading state when submitting form
- Location: tests\login-page.spec.ts:187:5

# Error details

```
Error: expect(locator).toBeDisabled() failed

Locator:  locator('button[type="submit"]')
Expected: disabled
Received: enabled
Timeout:  5000ms

Call log:
  - Expect "toBeDisabled" with timeout 5000ms
  - waiting for locator('button[type="submit"]')
    13 × locator resolved to <button type="submit" data-slot="button" class="inline-flex items-center justify-center gap-2 whitespace-nowrap rounded-md disabled:pointer-events-none disabled:opacity-50 [&_svg]:pointer-events-none [&_svg:not([class*='size-'])]:size-4 shrink-0 [&_svg]:shrink-0 outline-none focus-visible:border-ring focus-visible:ring-ring/50 focus-visible:ring-[3px] aria-invalid:ring-destructive/20 dark:aria-invalid:ring-destructive/40 aria-invalid:border-destructive hover:bg-primary/90 px-4 py-2 has-[>svg]:px-3 w-f…>Sign In</button>
       - unexpected value "enabled"

```

```yaml
- button "Sign In"
```

# Test source

```ts
  98  |     test('should display lock icon for password field', async ({ page }) => {
  99  |       const passwordIcon = page.locator('input[id="password"]').locator('..').locator('svg').first();
  100 |       await expect(passwordIcon).toBeVisible();
  101 |     });
  102 |   });
  103 | 
  104 |   test.describe('Submit Button', () => {
  105 |     test('should display Sign In button', async ({ page }) => {
  106 |       const submitButton = page.locator('button[type="submit"]');
  107 |       await expect(submitButton).toBeVisible();
  108 |       await expect(submitButton).toContainText('Sign In');
  109 |       await expect(submitButton).toBeEnabled();
  110 |     });
  111 | 
  112 |     test('should have gradient styling on submit button', async ({ page }) => {
  113 |       const submitButton = page.locator('button[type="submit"]');
  114 |       const className = await submitButton.getAttribute('class');
  115 |       expect(className).toContain('bg-gradient-to-r');
  116 |       expect(className).toContain('from-primary');
  117 |       expect(className).toContain('to-blue-600');
  118 |     });
  119 |   });
  120 | 
  121 |   test.describe('Password Visibility Toggle', () => {
  122 |     test('should toggle password visibility when clicking eye icon', async ({ page }) => {
  123 |       const passwordInput = page.locator('input[id="password"]');
  124 |       const toggleButton = page.locator('button[type="button"]').filter({ has: page.locator('svg') });
  125 |       
  126 |       // Initial state - password should be hidden
  127 |       await expect(passwordInput).toHaveAttribute('type', 'password');
  128 |       
  129 |       // Click toggle button
  130 |       await toggleButton.click();
  131 |       
  132 |       // Password should now be visible
  133 |       await expect(passwordInput).toHaveAttribute('type', 'text');
  134 |       
  135 |       // Click again to hide
  136 |       await toggleButton.click();
  137 |       
  138 |       // Password should be hidden again
  139 |       await expect(passwordInput).toHaveAttribute('type', 'password');
  140 |     });
  141 | 
  142 |     test('should show eye icon initially', async ({ page }) => {
  143 |       const eyeIcon = page.locator('button[type="button"]').filter({ has: page.locator('svg') }).locator('svg');
  144 |       await expect(eyeIcon).toBeVisible();
  145 |     });
  146 |   });
  147 | 
  148 |   test.describe('Form Validation', () => {
  149 |     test('should show error when submitting empty form', async ({ page }) => {
  150 |       const submitButton = page.locator('button[type="submit"]');
  151 |       await submitButton.click();
  152 |       
  153 |       // Should show toast error
  154 |       const toast = page.locator('[data-sonner-toast]');
  155 |       await expect(toast).toBeVisible();
  156 |       await expect(toast).toContainText('Please fill in all fields');
  157 |     });
  158 | 
  159 |     test('should show error when submitting only email', async ({ page }) => {
  160 |       const emailInput = page.locator('input[id="email"]');
  161 |       await emailInput.fill('test@example.com');
  162 |       
  163 |       const submitButton = page.locator('button[type="submit"]');
  164 |       await submitButton.click();
  165 |       
  166 |       // Should show toast error
  167 |       const toast = page.locator('[data-sonner-toast]');
  168 |       await expect(toast).toBeVisible();
  169 |       await expect(toast).toContainText('Please fill in all fields');
  170 |     });
  171 | 
  172 |     test('should show error when submitting only password', async ({ page }) => {
  173 |       const passwordInput = page.locator('input[id="password"]');
  174 |       await passwordInput.fill('password123');
  175 |       
  176 |       const submitButton = page.locator('button[type="submit"]');
  177 |       await submitButton.click();
  178 |       
  179 |       // Should show toast error
  180 |       const toast = page.locator('[data-sonner-toast]');
  181 |       await expect(toast).toBeVisible();
  182 |       await expect(toast).toContainText('Please fill in all fields');
  183 |     });
  184 |   });
  185 | 
  186 |   test.describe('Loading States', () => {
  187 |     test('should show loading state when submitting form', async ({ page }) => {
  188 |       const emailInput = page.locator('input[id="email"]');
  189 |       const passwordInput = page.locator('input[id="password"]');
  190 |       const submitButton = page.locator('button[type="submit"]');
  191 |       
  192 |       await emailInput.fill('test@example.com');
  193 |       await passwordInput.fill('password123');
  194 |       
  195 |       await submitButton.click();
  196 |       
  197 |       // Button should be disabled during loading
> 198 |       await expect(submitButton).toBeDisabled();
      |                                  ^ Error: expect(locator).toBeDisabled() failed
  199 |       await expect(submitButton).toContainText('Signing in...');
  200 |       
  201 |       // Should show spinner
  202 |       const spinner = submitButton.locator('svg.animate-spin');
  203 |       await expect(spinner).toBeVisible();
  204 |     });
  205 |   });
  206 | 
  207 |   test.describe('Accessibility', () => {
  208 |     test('should have proper autocomplete attributes', async ({ page }) => {
  209 |       const emailInput = page.locator('input[id="email"]');
  210 |       const passwordInput = page.locator('input[id="password"]');
  211 |       
  212 |       await expect(emailInput).toHaveAttribute('autocomplete', 'email');
  213 |       await expect(passwordInput).toHaveAttribute('autocomplete', 'current-password');
  214 |     });
  215 | 
  216 |     test('should have proper label associations', async ({ page }) => {
  217 |       const emailLabel = page.locator('label[for="email"]');
  218 |       const passwordLabel = page.locator('label[for="password"]');
  219 |       
  220 |       await expect(emailLabel).toBeVisible();
  221 |       await expect(passwordLabel).toBeVisible();
  222 |       
  223 |       const emailInput = page.locator('input[id="email"]');
  224 |       const passwordInput = page.locator('input[id="password"]');
  225 |       
  226 |       await expect(emailInput).toBeVisible();
  227 |       await expect(passwordInput).toBeVisible();
  228 |     });
  229 | 
  230 |     test('should support keyboard navigation', async ({ page }) => {
  231 |       // Tab through form elements
  232 |       await page.keyboard.press('Tab');
  233 |       const emailInput = page.locator('input[id="email"]');
  234 |       await expect(emailInput).toBeFocused();
  235 |       
  236 |       await page.keyboard.press('Tab');
  237 |       const passwordInput = page.locator('input[id="password"]');
  238 |       await expect(passwordInput).toBeFocused();
  239 |       
  240 |       await page.keyboard.press('Tab');
  241 |       const submitButton = page.locator('button[type="submit"]');
  242 |       await expect(submitButton).toBeFocused();
  243 |     });
  244 |   });
  245 | 
  246 |   test.describe('API Integration', () => {
  247 |     test('should call login API with correct credentials', async ({ page }) => {
  248 |       const emailInput = page.locator('input[id="email"]');
  249 |       const passwordInput = page.locator('input[id="password"]');
  250 |       const submitButton = page.locator('button[type="submit"]');
  251 |       
  252 |       await emailInput.fill('admin@factssolution.com');
  253 |       await passwordInput.fill('admin123');
  254 |       
  255 |       // Listen for API request
  256 |       const [request] = await Promise.all([
  257 |         page.waitForRequest('**/api/v1/auth/login'),
  258 |         submitButton.click()
  259 |       ]);
  260 |       
  261 |       // Verify request method
  262 |       expect(request.method()).toBe('POST');
  263 |       
  264 |       // Verify request payload
  265 |       const postData = request.postDataJSON();
  266 |       expect(postData.email).toBe('admin@factssolution.com');
  267 |       expect(postData.password).toBe('admin123');
  268 |     });
  269 | 
  270 |     test('should handle successful login', async ({ page }) => {
  271 |       const emailInput = page.locator('input[id="email"]');
  272 |       const passwordInput = page.locator('input[id="password"]');
  273 |       const submitButton = page.locator('button[type="submit"]');
  274 |       
  275 |       await emailInput.fill('admin@factssolution.com');
  276 |       await passwordInput.fill('admin123');
  277 |       
  278 |       await submitButton.click();
  279 |       
  280 |       // Wait for success toast
  281 |       const toast = page.locator('[data-sonner-toast]');
  282 |       await expect(toast).toBeVisible({ timeout: 10000 });
  283 |       await expect(toast).toContainText('Login successful!');
  284 |     });
  285 | 
  286 |     test('should handle invalid credentials', async ({ page }) => {
  287 |       const emailInput = page.locator('input[id="email"]');
  288 |       const passwordInput = page.locator('input[id="password"]');
  289 |       const submitButton = page.locator('button[type="submit"]');
  290 |       
  291 |       await emailInput.fill('invalid@example.com');
  292 |       await passwordInput.fill('wrongpassword');
  293 |       
  294 |       await submitButton.click();
  295 |       
  296 |       // Wait for error toast
  297 |       const toast = page.locator('[data-sonner-toast]');
  298 |       await expect(toast).toBeVisible({ timeout: 10000 });
```